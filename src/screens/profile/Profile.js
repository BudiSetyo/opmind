import React from 'react';
import {ScrollView, View, Text} from 'react-native';

const Profile = () => {
  return (
    <ScrollView>
      <View>
        <Text>Profile</Text>
      </View>
    </ScrollView>
  );
};

export default Profile;
